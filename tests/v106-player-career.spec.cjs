'use strict';
// Retired in V107 — replaced by tests/v107-player-career-shell.spec.cjs.
// Player Career now runs inside the manager shell, so the old screens, class
// names and screenshot baselines this suite asserted no longer exist.
const {test}=require('@playwright/test');
test.skip('V106 player-career suite retired; see v107-player-career-shell.spec.cjs',()=>{});
